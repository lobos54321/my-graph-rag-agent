# 前端 Dockerfile
FROM node:18-alpine

WORKDIR /app

# 复制前端文件
COPY package*.json ./
COPY next.config.js ./
COPY tsconfig.json ./
COPY pages/ ./pages/
COPY components/ ./components/
COPY public/ ./public/

# 安装依赖
RUN npm install

# 构建应用
RUN npm run build

# 启动应用
EXPOSE 3000
CMD ["npm", "start"]